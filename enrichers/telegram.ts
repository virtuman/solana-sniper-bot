export const searchTelegram = (telegramNickname: string) => {
  // const params = { q: `solana AND (${tokenName})`, count: 100 };
  // client.get('search/tweets', params, function (error, tweets, response) {
  //   if (!error) {
  //     console.log(tweets);
  //   }
  // });
};
